@model IEnumerable<Vaxtaskra.Models.ProductCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.is_individual_product)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductType.Type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.is_individual_product)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductType.Type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductCategoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductCategoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductCategoryID })
        </td>
    </tr>
}

</table>
