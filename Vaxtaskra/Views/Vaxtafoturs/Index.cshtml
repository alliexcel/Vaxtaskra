@model IEnumerable<Vaxtaskra.Models.Vaxtafotur>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Heiti)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Is_Indexed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.is_active)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Heiti)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Is_Indexed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.is_active)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VaxtafoturID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VaxtafoturID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VaxtafoturID })
        </td>
    </tr>
}

</table>
